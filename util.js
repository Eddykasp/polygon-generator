module.exports = {
  clip(poly, mask){
  /*
   * This utility function takes an arbitrary polygon and a convex polygon
   * and returns a new polygon, which is the first polygon clipped with the
   * mask polygon using the Sutherland-Hodgman algorithm.
   */
  }
};
